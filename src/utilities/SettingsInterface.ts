export default interface SettingsInterface {
  default_appointment_booking_threshold: number;
  default_appointment_duration: number;
  language: object;
  name: string;
  primary_colour: string;
  secondary_colour: string;
}
